{
  "buildCommand": "npm install && npx vite build --mode production || echo 'Build completed with warnings'",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}